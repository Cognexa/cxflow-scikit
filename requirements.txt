# common/   System packages (usually with `python-` prefix)
# ---------------------------------------------------------
matplotlib>=2.0.0
numpy>=1.13.0
scipy>=0.19.0
pandas>=0.19.2
scikit-learn>=0.18.1
pip>=9.0.0

# aur/      Less common system packages (usually with `python-` prefix)
# ---------------------------------------------------------------------

# pip       The previous options are not available or broken
# ----------------------------------------------------------
typing>=3.5.3.0
